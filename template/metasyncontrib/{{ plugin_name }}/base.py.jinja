"""Base class for all {{ plugin_name }} distributions."""


def metadist_{{ package_name }}():
    """Decorate class to create a distribution with disclosure control.

    Returns
    -------
    cls:
        Class with the appropriate class variables.
    """
    def _wrap(cls):
        cls.provenance = "{{ plugin_name }}"
        cls.privacy = "{{ privacy }}"
        return cls
    return _wrap
