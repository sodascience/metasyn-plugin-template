"""Module contains distribution provider for {{ plugin_name|lower }}."""

from __future__ import annotations
from metasyn.provider import BaseDistributionProvider
from metasyn.distribution.base import BaseDistribution


class {{ prefix }}Provider(BaseDistributionProvider):
    """Distribution provider that contains safe distributions using {{ plugin_name|lower }}.
    """

    @property
    def name(self):
        return "{{plugin_name}}"

    @property
    def version(self):
        return "1.0"

    @property
    def distributions(self) -> list[type[BaseDistribution]]:
        return [
            # Add your distributions here.
        ]
